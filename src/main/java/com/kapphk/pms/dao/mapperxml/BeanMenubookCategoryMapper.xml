<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kapphk.pms.dao.mapper.BeanMenubookCategoryMapper" >
    <resultMap id="BaseResultMap" type="com.kapphk.pms.bean.BeanMenubookCategory" >
        <!--  自动生成 2016年01月22日 表与实体类字段对应 -->
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="menu_id" property="menuId" jdbcType="BIGINT" />
        <result column="category_id" property="categoryId" jdbcType="BIGINT" />
        <result column="create_time" property="createTime" jdbcType="VARCHAR" />
    </resultMap>
    
    <!-- 根据菜单id，删除步骤 huangzh 2016-01-23 -->
    <delete id="deleteByMenuId" parameterType="java.lang.Long" >
        <!--  自动生成 2016年01月22日 删除数据 -->
        delete from sys_menubook_category
        where menu_id = #{menuid,jdbcType=BIGINT}
    </delete>
    
    
    <sql id="Base_Column_List" >
        <!--  自动生成 2016年01月22日 表字段名称 -->
        id, menu_id, category_id, create_time
    </sql>
    
    <sql id="findByPageSql" >
        <!-- WARNING -  自动生成 2016-01-22  分页查询SQL -->
        select a.* from sys_menubook_category a
        <where >
            <if test="param.id != null " >
                and a.id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.menuId != null " >
                and a.menu_id = #{param.menuId,jdbcType=BIGINT}
            </if>
            <if test="param.categoryId != null " >
                and a.category_id = #{param.categoryId,jdbcType=BIGINT}
            </if>
        </where>
        order by a.id desc
    </sql>
    
    <select id="findByPage" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-01-22  分页查询 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp limit #{offset}, #{pageSize}
    </select>
    
    <select id="findByPageCount" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-01-22  分页查询总条数 -->
        select count(1) from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
    
    <select id="findById" resultMap="BaseResultMap" parameterType="java.lang.Long" >
        <!--  自动生成 2016年01月22日 根据ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from sys_menubook_category
        where id = #{id,jdbcType=BIGINT}
    </select>
    
    <select id="load" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-01-22  根据多个ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from sys_menubook_category where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </select>
    
    <select id="all" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-01-22  查询所有 -->
        select 
        <include refid="Base_Column_List" />
        from sys_menubook_category
    </select>
    
    <select id="count" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-01-22 查询总条数 -->
        select count(1) from sys_menubook_category
    </select>
    
    <update id="update" parameterType="com.kapphk.pms.bean.BeanMenubookCategory" >
        <!--  自动生成 2016年01月22日 更新数据 -->
        update sys_menubook_category
        <set >
            <if test="menuId != null" >
                menu_id = #{menuId,jdbcType=BIGINT},
            </if>
            <if test="categoryId != null" >
                category_id = #{categoryId,jdbcType=BIGINT},
            </if>
            <if test="createTime != null" >
                create_time = #{createTime,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>
    
    <insert id="insert" parameterType="com.kapphk.pms.bean.BeanMenubookCategory" useGeneratedKeys="true" keyProperty="id" >
        <!--  自动生成 2016年01月22日 添加数据 -->
        insert into sys_menubook_category
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="menuId != null" >
                menu_id,
            </if>
            <if test="categoryId != null" >
                category_id,
            </if>
            <if test="createTime != null" >
                create_time,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="menuId != null" >
                #{menuId,jdbcType=BIGINT},
            </if>
            <if test="categoryId != null" >
                #{categoryId,jdbcType=BIGINT},
            </if>
            <if test="createTime != null" >
                #{createTime,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>
    
    <insert id="inserts" parameterType="java.util.List" >
        <!-- WARNING -  自动生成 2016-01-22 添加多条数据 -->
        insert into sys_menubook_category (menu_id, category_id, create_time)
        values
        <foreach collection="list" item="item" separator="," >
            (#{item.menuId,jdbcType=BIGINT}, #{item.categoryId,jdbcType=BIGINT}, #{item.createTime,jdbcType=VARCHAR})
        </foreach>
    </insert>
    
    <delete id="delete" parameterType="java.lang.Long" >
        <!--  自动生成 2016年01月22日 删除数据 -->
        delete from sys_menubook_category
        where id = #{id,jdbcType=BIGINT}
    </delete>
    
    <delete id="deletes" >
        <!-- WARNING -  自动生成 2016-01-22 删除多条数据 -->
        delete from sys_menubook_category where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </delete>
    
    <delete id="deleteByEntity" >
        <!-- WARNING -  自动生成 2016-01-22 通过实体类中的条件删除数据 -->
        delete from sys_menubook_category
        <where >
            <if test="param.id != null " >
                and id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.menuId != null " >
                and menu_id = #{param.menuId,jdbcType=BIGINT}
            </if>
            <if test="param.categoryId != null " >
                and category_id = #{param.categoryId,jdbcType=BIGINT}
            </if>
        </where>
    </delete>
    
    <select id="findAll" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-01-22  通过实体中的条件查询所有 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
</mapper>