<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kapphk.pms.dao.mapper.BeanDistrctMapper" >
    <resultMap id="BaseResultMap" type="com.kapphk.pms.bean.BeanDistrct" >
        <!--  自动生成 2016年01月28日 表与实体类字段对应 -->
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="distrct" property="distrct" jdbcType="VARCHAR" />
        <result column="cid" property="cid" jdbcType="BIGINT" />
    </resultMap>
    
    <sql id="Base_Column_List" >
        <!--  自动生成 2016年01月28日 表字段名称 -->
        id, distrct, cid
    </sql>
    
    <sql id="findByPageSql" >
        <!-- WARNING -  自动生成 2016-01-28  分页查询SQL -->
        select a.* from sys_distrct_info a
        <where >
            <if test="param.id != null " >
                and a.id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.distrct != null and param.distrct != ''" >
                and a.distrct = #{param.distrct,jdbcType=VARCHAR}
            </if>
            <if test="param.cid != null " >
                and a.cid = #{param.cid,jdbcType=BIGINT}
            </if>
        </where>
        order by a.id desc
    </sql>
    
    <select id="findByPage" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-01-28  分页查询 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp limit #{offset}, #{pageSize}
    </select>
    
    <select id="findByPageCount" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-01-28  分页查询总条数 -->
        select count(1) from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
    
    <select id="findById" resultMap="BaseResultMap" parameterType="java.lang.Long" >
        <!--  自动生成 2016年01月28日 根据ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from sys_distrct_info
        where id = #{id,jdbcType=BIGINT}
    </select>
    
    <select id="load" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-01-28  根据多个ID查询 -->
        select 
        <include refid="Base_Column_List" />
        from sys_distrct_info where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </select>
    
    <select id="all" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-01-28  查询所有 -->
        select 
        <include refid="Base_Column_List" />
        from sys_distrct_info
    </select>
    
    <select id="count" resultType="java.lang.Integer" >
        <!-- WARNING -  自动生成 2016-01-28 查询总条数 -->
        select count(1) from sys_distrct_info
    </select>
    
    <update id="update" parameterType="com.kapphk.pms.bean.BeanDistrct" >
        <!--  自动生成 2016年01月28日 更新数据 -->
        update sys_distrct_info
        <set >
            <if test="distrct != null" >
                distrct = #{distrct,jdbcType=VARCHAR},
            </if>
            <if test="cid != null" >
                cid = #{cid,jdbcType=BIGINT},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>
    
    <insert id="insert" parameterType="com.kapphk.pms.bean.BeanDistrct" useGeneratedKeys="true" keyProperty="id" >
        <!--  自动生成 2016年01月28日 添加数据 -->
        insert into sys_distrct_info
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="distrct != null" >
                distrct,
            </if>
            <if test="cid != null" >
                cid,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="distrct != null" >
                #{distrct,jdbcType=VARCHAR},
            </if>
            <if test="cid != null" >
                #{cid,jdbcType=BIGINT},
            </if>
        </trim>
    </insert>
    
    <insert id="inserts" parameterType="java.util.List" >
        <!-- WARNING -  自动生成 2016-01-28 添加多条数据 -->
        insert into sys_distrct_info (distrct, cid)
        values
        <foreach collection="list" item="item" separator="," >
            (#{item.distrct,jdbcType=VARCHAR}, #{item.cid,jdbcType=BIGINT})
        </foreach>
    </insert>
    
    <delete id="delete" parameterType="java.lang.Long" >
        <!--  自动生成 2016年01月28日 删除数据 -->
        delete from sys_distrct_info
        where id = #{id,jdbcType=BIGINT}
    </delete>
    
    <delete id="deletes" >
        <!-- WARNING -  自动生成 2016-01-28 删除多条数据 -->
        delete from sys_distrct_info where id in 
        <foreach collection="ids" item="item" open="(" separator="," close=")" >
            #{item}
        </foreach>
    </delete>
    
    <delete id="deleteByEntity" >
        <!-- WARNING -  自动生成 2016-01-28 通过实体类中的条件删除数据 -->
        delete from sys_distrct_info
        <where >
            <if test="param.id != null " >
                and id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.distrct != null and param.distrct != ''" >
                and distrct = #{param.distrct,jdbcType=VARCHAR}
            </if>
            <if test="param.cid != null " >
                and cid = #{param.cid,jdbcType=BIGINT}
            </if>
        </where>
    </delete>
    
    <select id="findAll" resultMap="BaseResultMap" >
        <!-- WARNING -  自动生成 2016-01-28  通过实体中的条件查询所有 -->
        select temp.* from (
        <include refid="findByPageSql" />
        ) temp 
    </select>
    
     <select id="findDistrctByCondition" resultMap="BaseResultMap" >
        select   *   from sys_distrct_info
        <where >
            <if test="param.id != null " >
                and id = #{param.id,jdbcType=BIGINT}
            </if>
            <if test="param.distrct != null and param.distrct != ''" >
                and distrct = #{param.distrct,jdbcType=VARCHAR}
            </if>
            <if test="param.cid != null " >
                and cid = #{param.cid,jdbcType=BIGINT}
            </if>
        </where>
    </select>
    
</mapper>